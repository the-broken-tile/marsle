export default class Countdown {
    private readonly time;
    private readonly container;
    private readonly element;
    constructor(time: number, container: HTMLDivElement);
    private getTime;
    private tick;
    private text;
    private pad;
}
